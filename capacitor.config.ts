import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.christcollective.project',
  appName: 'rest-express',
  webDir: 'dist'
};

export default config;
